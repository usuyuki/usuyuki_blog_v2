---
import { SITE_URL } from "~/consts";

interface Props {
  placeholder?: string;
  className?: string;
}

const { placeholder = "検索...", className = "" } = Astro.props;
// サイト内検索のスコープはSITE_URLのホスト名から導出する(ドメイン移行時に自動追従させるため)
const searchHostname = new URL(SITE_URL).hostname;
---

<form
	class={`flex w-full items-stretch border border-ink ${className}`}
	action="https://www.google.com/search"
	method="get"
	target="_blank"
	role="search"
>
	<input type="hidden" name="sitesearch" value={searchHostname} />
	<input
		type="text"
		name="q"
		placeholder={placeholder}
		class="min-w-0 flex-1 bg-paper px-3 py-1.5 text-sm text-ink placeholder:text-[var(--color-muted)] outline-none"
		required
	/>
	<button
		type="submit"
		class="shrink-0 bg-ink px-3 font-en text-xs font-bold tracking-[0.18em] text-paper transition-colors duration-150 hover:bg-orange"
	>
		SEARCH
	</button>
</form>
